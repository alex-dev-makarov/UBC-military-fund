node_modules
dist
.wrangler
*.local
.env
.env.*
!.env.example
api/dist
api/node_modules
.jarvis
docs/
api/data/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# macOS finder metadata and the TypeScript incremental build cache.
# Both were committed by accident in the first commit; the rules keep them out.
.DS_Store
*.tsbuildinfo
